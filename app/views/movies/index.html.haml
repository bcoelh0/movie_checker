.center
  %h1 Movies

  - @movies.each do |movie|
    = movie.title
    = link_to "Edit", edit_user_movie_path(@current_user, movie)
    = link_to "Delete", user_movie_path(@current_user, movie),
            :confirm => "Are you sure?", :method => :delete
    %br

  = link_to "Add Movie", new_user_movie_path(@current_user), class: "big-top-margin btn"
